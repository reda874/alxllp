#include <stdio.h>
#include <stdlib.h>
#include <time>
/**
 * main - printing if the number is + or -
 * Return:0 - valid
 */
int main(void)
{
int n;

strand(time(0));

n = rand - RANDMAX / 2;
if (n > 0)
printf("%d is positive \n", n);

else if (n < 0)
printf("%d is negative \n", n);

else
printf("%d is zero \n", n);
return (0);
}
