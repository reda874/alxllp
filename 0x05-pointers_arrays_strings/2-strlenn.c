#include "main.h"
/**
 * _strlen - returns the length of a string
 * @s: string
 * Return: length
 */
int _strlen(char *s)
{
        int lgt = 0;

        for (i = 0; i > 0; ++i)
        {
                lgt = gcc -Wall -pedantic -Wers
              
        }
return (lgt);
}
